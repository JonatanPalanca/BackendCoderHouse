<div>
  <h1>Tu perfil</h1>
  <p>{{user.name}}</p>
  <p>{{user.email}}</p>
  <p>{{user.age}}</p>
  <p>{{user.role}}</p>
  <!-- Muestra el rol -->
  <a href="/api/sessions/logout"><button>Salir</button></a>
</div>